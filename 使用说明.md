# Spine 序列帧导入工具 — 使用说明

---

## 一、安装与启动

### 安装版（推荐）

双击 `SpineSequenceExporter-0.1.0-Setup.exe`，按提示完成安装。安装完成后从桌面快捷方式或开始菜单启动。

### 便携版（免安装）

解压 `SpineSequenceExporter-0.1.0-Portable.zip` 到任意目录，直接运行 `SpineSequenceExporter.exe`。

### 网页版（离线 HTML）

双击 `SpineSequenceExporter.html` 即可在浏览器中打开使用，无需安装任何环境。

---

## 二、界面总览

启动后界面分为两部分：

- **左侧边栏**：功能导航，包含「序列帧导入」和「JSON 分析与修复」两个模块
- **右侧主区域**：具体的操作界面

---

## 三、序列帧导入

将 PNG 序列帧图片批量转换为 Spine 可识别的 JSON 动画数据。

### 3.1 添加图片

有两种方式添加图片：

1. **点击上传区域**，在弹出的文件选择器中选择 PNG 图片（支持多选）
2. **直接拖拽** PNG 文件到上传区域

图片加载后会自动按文件名**自然排序**（例如 `frame1` → `frame2` → `frame10`，而非 `frame1` → `frame10` → `frame2`），并以缩略图网格的形式展示预览。每张缩略图左下角显示序号，底部显示分辨率。

> **追加图片**：已有图片的情况下，继续拖入或点击上传区域可追加新图片，自动与已有图片合并并去重。

> **清空图片**：点击预览区域右上角的「清空」按钮可移除所有已加载的图片。

### 3.2 配置参数

#### 导入模式

| 模式 | 说明 | 适用场景 |
|------|------|---------|
| **新建项目** | 生成完整的 Spine 骨架 JSON（含 skeleton、bones、slots、skins、animations） | 从零开始创建一个新的序列帧动画 |
| **合并到现有骨架** | 仅生成动画和附件数据，需要指定现有骨架中的 Slot、Bone、Skin 名称 | 将序列帧动画合并到已有的 Spine 项目中 |

#### 通用参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| **Spine 兼容版本** | `3.8.99` | 目标 Spine 编辑器版本号，影响 JSON 格式（3.8 和 4.x 的 skins 结构不同） |
| **动画名称** | `sequence` | 生成的动画在 JSON 中的名称 |
| **FPS（帧率）** | `30` | 每秒播放帧数，决定每帧的时间间隔 |
| **图片路径** | `./images/` | JSON 中 `imagesPath` 字段的值，Spine 加载图片时的相对路径前缀 |
| **附件命名策略** | 去扩展名 | 「去扩展名」使用文件名去掉 `.png` 后缀作为附件名；「保留完整文件名」保留 `.png` 后缀 |
| **附件重名处理** | 报错并停止 | 当多张图片产生相同附件名时：「报错并停止」会提示冲突；「自动重命名」会自动添加 `_1`、`_2` 后缀 |

#### 合并模式专属参数

选择「合并到现有骨架」模式时，需要额外填写以下参数，**必须与目标骨架中的名称完全一致**：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| **目标 Slot 名称** | `frameSlot` | 现有骨架中承载帧动画的 slot 名 |
| **目标 Skin 名称** | `default` | 现有骨架中的 skin 名 |
| **目标 Bone 名称** | `root` | 现有骨架中挂载的 bone 名 |

### 3.3 导出

点击「📦 导出 JSON」按钮，工具会：

1. 校验所有参数，有问题时在界面顶部显示红色错误提示
2. 校验通过后，**自动弹出保存对话框**，将生成的 JSON 文件保存到你选择的位置
3. 同时在界面下方展开「JSON 预览」区域，可以查看生成的完整 JSON 内容

导出的 JSON 文件命名默认为 Skeleton 名称（如 `SequenceImport.json`）。

---

## 四、JSON 分析与修复

对已有的 Spine JSON 文件进行结构检查，找出 Slot 引用问题并提供自动修复。

### 4.1 加载 JSON 文件

点击上传区域或直接拖入 `.json` 文件。工具会自动解析并生成分析报告。

### 4.2 分析报告

分析报告包含以下内容：

**统计卡片**（顶部三个数字面板）：
- **已定义 slots**：JSON 的 `slots` 数组中声明的所有 slot
- **被引用 slots**：在 skins、animations、drawOrder、constraints 中被引用的 slot
- **缺失 slots**：被引用但未定义的 slot（红色数字表示有问题，绿色表示正常）

**详细列表**：
- 已定义的 slots（绿色标签）
- 被引用的 slots（蓝色标签）
- 缺失的 slots（红色标签，如果有的话）
- 非法引用位置（列出具体引用路径，如 `animations.idle.slots.missingSlot`）
- 完整文字分析报告（可折叠查看）

> 如果所有引用均正常，会显示绿色提示：「✓ 未发现缺失的 slot 引用，JSON 结构完整」

### 4.3 修复

当检测到缺失 slots 时，界面底部会出现「修复方案」卡片，提供两种修复方式：

| 按钮 | 功能 | 说明 |
|------|------|------|
| **➕ 补全缺失的 slots** | 在 `slots` 数组中添加缺失的 slot 定义 | slot 会挂载到 `root` bone，attachment 为 null |
| **🗑️ 删除非法引用** | 移除引用了不存在 slot 的所有条目 | 会从 skins、animations、drawOrder、constraints 中清除无效引用 |

点击任一按钮后，界面会展示修复后的 JSON 内容。确认无误后点击「💾 下载修复后的 JSON」保存文件。

---

## 五、参数速查

### 新建项目模式的最小配置

只需完成以下步骤即可导出：
1. 添加 PNG 图片
2. 点击「导出 JSON」（其余参数使用默认值即可）

### 常用 Spine 版本号

| 版本 | 说明 |
|------|------|
| `3.8.99` | Spine 3.8.x 系列（默认） |
| `4.0.64` | Spine 4.0.x 系列 |
| `4.1.0` | Spine 4.1.x 系列 |
| `4.2.0` | Spine 4.2.x 系列 |

> 版本号会影响 skins 的数据结构格式：3.8 使用数组格式，4.x 使用对象格式。请根据你使用的 Spine 编辑器版本填写。

---

## 六、常见问题

**Q：拖入图片后没有反应？**
请确认图片格式为 PNG。工具仅支持 `.png` 格式的文件，拖入其他格式会被忽略并提示错误。

**Q：导出时提示「存在重复的附件名称」？**
多张图片去掉扩展名后名称相同（如 `frame.png` 和 `frame.jpg`，但本工具仅接受 PNG，所以通常不会出现此问题；但如果两张 PNG 文件名完全相同则会触发）。可以在「附件重名处理」中选择「自动重命名」来解决。

**Q：导出的 JSON 在 Spine 中打开报错？**
1. 确认「Spine 兼容版本」与你使用的 Spine 编辑器版本匹配
2. 确认图片文件已放入 JSON 中 `imagesPath` 指定的目录

**Q：合并模式下 Spine 提示 "Slot not found"？**
检查目标 Slot、Skin、Bone 名称是否与现有骨架中的完全一致（区分大小写）。

**Q：网页版和桌面版有区别吗？**
功能完全一致。网页版可直接在浏览器中打开使用，无需安装；桌面版提供独立的窗口体验。
